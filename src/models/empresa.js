export default class Empresa {
  constructor(empresa, ruc, comprobante, clientes) {
    this.empresa = empresa;
    this.ruc = ruc;
    this.comprobante = comprobante;
    this.clientes = clientes;
  }
}
